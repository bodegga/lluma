# Lluma — Production Infrastructure Inventory & Runbook

> Single source of truth for where Lluma runs and how to deploy it. Live IPs/hostnames are
> filled in as components are provisioned (search `TBD`). **No secrets live in this file** —
> credentials are in `~/.lluma-deploy.env` (operator machine) and key material in
> `~/.lluma-keys-prod` (offline); neither is committed.

## Accounts & control planes

| Thing | Value | Access |
|---|---|---|
| Source repo | `github.com/bodegga/lluma` (public) | `gh` CLI (authenticated) |
| Cloud A (gateway/broker + site) | **DigitalOcean** — `shake707@gmail.com` ("My Team"), droplet limit 15 | `doctl` (authenticated) |
| Cloud B (relay) | **Vultr** | `VULTR_API_KEY` in `~/.lluma-deploy.env` |
| DNS | **ZoneEdit** (`dns1/2/3.zoneedit.com`), domain `bodegga.net` | `ZONEEDIT_USER`/`ZONEEDIT_TOKEN` in `~/.lluma-deploy.env` |
| Marketing site | DO App Platform app **`lluma-web`** (`c8effc7b-a326-4768-b9fa-c37cbb6e1ee7`) → `lluma.bodegga.net` (ingress `lluma-web-kduhr.ondigitalocean.app`) | `doctl apps` |
| Deploy SSH key | **`lluma-deploy-claude`** on DO (fp `5a:4f:42:9a:14:4f:0e:d5:45:75:8a:bf:e3:11:cf:2c`); local private key `~/.ssh/id_ed25519` | — |

## Two-provider topology (ADR-0002; leak L10 — no vendor sees both edges)

```
client ──TLS──▶  RELAY (Vultr)  ──▶  GATEWAY + co-located ISSUER/BROKER origin (DigitalOcean)
                 sees IP + OHTTP        gateway decapsulates OHTTP, path-allowlists;
                 ciphertext only        broker is sole redeemer; origin bound to loopback
                                              │
                                    broker ──▶ serving hosts (register via ingress; PoW + admission)
```

## Hosts (live)

| Role | Provider | Name | Public IP | Ports | Status |
|---|---|---|---|---|---|
| Relay (VPS-A) | Vultr (ord) | `lluma-relay` | `64.177.112.245` | `8780` (public, ufw) | **live** |
| Gateway + broker origin (VPS-B) | DigitalOcean (nyc3) | `lluma-broker` | `159.65.35.137` | `8782` gateway, `8081` ingress, `8080` core (loopback) | **live** |
| Echo serving host (demo) | co-located on VPS-B | `lluma-host` | `159.65.35.137` | `9000` | **live** (echo upstream; registered + admitted) |
| Marketing site | DO App Platform | `lluma-web` | (managed) | — | live → `lluma.bodegga.net` |

**Verified 2026-07-21:** a full anonymous inference completed end-to-end
(`client → relay → gateway → broker → host → back`) — blind-token issuance, OHTTP
relaying, PoW host registration + admission, spend-before-forward redeem, and E2E
sealing all exercised live. Reproduce with `cargo run -p lluma-client --example
live_smoke` (env in the example header; gateway key-config from
`journalctl -u lluma-gateway | grep key_config`).

**DO droplets that are NOT Lluma — do not touch:** `carflipper-automation`, `debian-…-sfo3-01`,
`wedding.biasi.co`, `trading-bot`, `bodegga-web` (147.182.175.148), `laws-ingest`.

## Ports / listeners

- Relay: `LLUMA_RELAY_BIND` (public, TLS-fronted) → forwards to the gateway.
- Gateway: `LLUMA_GATEWAY_BIND` (reachable from the relay only) → origin on loopback.
- Broker origin: core `127.0.0.1:8080` (issuer + exec + trial + snapshot), ingress
  `127.0.0.1:8081` (host register/heartbeat/receipt + admin). Loopback-bound; only the gateway
  reaches the core, and hosts reach ingress over the host network path.

## Key material (offline; NEVER committed)

Generated by `cargo run -p lluma-keygen -- <dir>` into `~/.lluma-keys-prod`:
`issuer_sk.der` / `issuer_pk.der` (RSA-BSSA), `registry.sk` (Ed25519, 32 B), `epoch_salt.bin`
(32 B, non-zero). Current **issuer `key_id`** (BLAKE3 of the public key, safe to publish):
`ad9c7bc7c8b1a3c59157a07f067e3a6d41217f52c8713d31a3295992873748cf`.

## Deploy procedure (summary; scripts in `ops/deploy/`)

1. `~/.lluma-deploy.env` holds `VULTR_API_KEY`, `ZONEEDIT_USER`, `ZONEEDIT_TOKEN`. Key material
   in `~/.lluma-keys-prod`.
2. **VPS-B (DO):** create droplet with the `lluma-deploy-claude` key → install Rust + build
   tools → clone `bodegga/lluma` (main) → `cargo build --release -p lluma-gateway -p lluma-broker`
   → scp key material to `/etc/lluma/keys` (0600) → fill `/etc/lluma/{broker,gateway}.env` →
   install `ops/deploy/systemd/*.service` → start broker then gateway → firewall (gateway port
   from relay only; origin loopback).
3. **VPS-A (Vultr):** create instance with the deploy key → build `lluma-relay` → set
   `LLUMA_RELAY_GATEWAY` to VPS-B → start relay → TLS-front it.
4. **DNS (ZoneEdit):** `relay.lluma.bodegga.net` → VPS-A, `gw.lluma.bodegga.net` → VPS-B; issue
   Let's Encrypt certs.
5. Register a serving host against the broker ingress (PoW + heartbeats to admission).
6. End-to-end smoke test with `lluma-client`; verify the relay sees only IP+ciphertext and the
   origin never logs prompt/response plaintext.
7. **Site:** `doctl apps create-deployment c8effc7b-…` after pushing site changes to `main`.

## Monitoring / ops

- `GET /admin/invariant` (broker ingress, `x-admin-secret`): `redeemed ≤ issued` must hold; a
  trip is a key-compromise alarm (the broker refuses redeem on trip).
- Backup: redb is one file (`LLUMA_DB_PATH`) — snapshot it from a consistent point.
- Rotate `epoch_salt` per epoch; keep `epoch_salt_prev` for the k−1 window (automation TBD).
- Pre-production hardening before real traffic: `docs/PHASE1-FOLLOWUPS.md` (spawn_blocking,
  snapshot cadence cache, gateway OHTTP key persistence).
